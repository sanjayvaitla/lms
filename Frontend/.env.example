# API base URL
# Dev: Vite proxies /api → http://localhost:6000
VITE_API_URL=/api/v1

# Production (same-origin behind nginx — proxy /api only; files come from S3 presigned URLs):
# VITE_API_URL=/api/v1
#
# Production (separate API host):
# VITE_API_URL=https://api.yourcompany.com/api/v1
